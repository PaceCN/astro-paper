{
  "name": "boke",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "astro": "astro",
    "db:generate": "drizzle-kit generate",
    "db:local": "wrangler d1 migrations apply blogdb --local",
    "db:remote": "wrangler d1 migrations apply blogdb --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "latest",
    "@tailwindcss/vite": "latest",
    "astro": "^6.3.3",
    "drizzle-orm": "latest",
    "hono": "latest",
    "jose": "latest",
    "marked": "latest",
    "sanitize-html": "latest",
    "tailwindcss": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/sanitize-html": "latest",
    "drizzle-kit": "latest",
    "wrangler": "latest"
  }
}
